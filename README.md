# React Redux Typescript Material-UI boilerplate

### For Local Development

- Clone the repo
- In the root of the repo run `npm i`
- Then run `npm start`

### Prerequisites

- Install Visual Studio Code **Prettier** extension
